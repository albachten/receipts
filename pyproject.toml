[project]
name = "expense-manager"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.12",
    "gunicorn>=25.3.0",
    "python-decouple>=3.8",
    "whitenoise>=6.12.0",
]

